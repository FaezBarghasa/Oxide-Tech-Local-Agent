[package]
name = "surface"
version = "0.1.0"
edition = "2024"
authors = ["Faez Barghasa"]
description = "Mutable Autoresearch training surface (rewritten train.py) compiled as cdylib"

[lib]
crate-type = ["cdylib"]

[dependencies]
surface-api = { path = "../../../crates/surface-api" }

# STRICT RULE: No new dependencies allowed in this mutable surface!
