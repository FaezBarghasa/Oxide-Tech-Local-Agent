{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Default capability: core APIs for the main window plus opener for external links.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
